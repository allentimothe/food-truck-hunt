<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Menu</title>
</head>
<body>
  <header>
    <div class="homeContainer">
        <div class="title">
    <h1>The Halal Guys</h1>
            </div>
            <div class="anchor">
        <h1><a href="/">Home</a></h1>
        <h1><a href="/users/dashboard">Dashboard</a></h1>
        <h1><a href="/users/logout">Signout</a></h1>
        </ul>
    </nav>
    </div>
</div>
</header>
<div class="availMain">
<fieldset class="availContainer">
<legend>Log</legend>
<table>
  <thead>
      <th>Employee Name</th>
      <th>Shift</th>
      <th>Hours Worked</th>
  </thead>
  <tbody>
      <% menus.forEach(function(m)  { %>
          <tr>
              <td><%= m.employeeName %></td>
              <td><%= m.shift %></td>
              <td><%= m.hours %></td>
              <td><a href="/menus/<%= m._id %>/edit">Edit</a></td>
              <td><form action="/menus/<%= m._id %>?_method=DELETE"
                class="delete-form" method="POST"><button type="submit">Delete</button></a></td>
          </tr>
      <% }) %>
  </tbody>
</table>
<!-- < %  menus.forEach(function(menu, idx) { %> -->
  <!-- <li>
    <form action="/menus/<  %= m._id %>?_method=DELETE"
      class="delete-form" method="POST">
      <button type="submit">x</button>
    </form>
    <a href="/menus/< %= m._id %>">
      < %= m._id %>
    </a>
      - 
  </li>
< % }); %>
</ul> -->
</fieldset>
</div>
</div>
 </body>
</html>
